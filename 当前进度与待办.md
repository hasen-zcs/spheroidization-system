# 当前进度与待办

**日期：** 2026-09-17
**范围：** 后端 ↔ 数据库打通、假数据清理、API 层接线

---

## 一、已完成

### 1. 数据库连接层

| 文件 | 作用 |
| --- | --- |
| `app/infrastructure/database/connection.py` | 建连接、建表。**全项目取连接只走 `connect()` 这一个入口** |
| `app/infrastructure/database/schema.sql` | DDL，3 张 STRICT 表：StandardRecord / SpheroidizationRecord / ReviewRecord |
| `data/spheroidization.db` | 数据库文件本体（SQLite 就是一个文件，没有服务器） |

关键点：`connect()` 里每次建连接都执行 `PRAGMA foreign_keys = ON`。SQLite 的外键默认是**关**的，而且是**每个连接**单独开——不打开的话 `ON DELETE RESTRICT` 全是摆设，而且不报任何错。

### 2. 仓储实现

`app/infrastructure/database/sqlite.py`，三个类：

| 类 | 方法 |
| --- | --- |
| `SqliteSpheroidizationRepository` | `save` / `update` / `get_by_id` / `list_all` / `list_abnormal` |
| `SqliteStandardRepository` | `save` / `get_current` / `get_history` |
| `SqliteReviewRepository` | `save` / `list_by_spheroid_id` |

### 3. API 层接通业务层

**11 个端点**（10 个业务 + `/api/health`），每个路由用 FastAPI 的 `Depends` 注入 UseCase，不再自己拼返回值、不再自己编号。

`app/dependencies.py` 是装配层：`Container` 把「连接 → 仓储 → UseCase」串起来，10 个 `get_xxx()` 供 FastAPI 注入。

### 4. 清理假数据

| 删掉的东西 | 原因 |
| --- | --- |
| `app/mock/`（整个目录） | 一开始为测试写的假数据 |
| `app/infrastructure/database/memory.py` | 内存版仓储，数据源就是 mock |
| `tests/test_usecases.py` | 建在假仓储上，且 import 了已删除的类，**导致 pytest 整体跑不起来** |
| `tests/test_*.py` 三个占位文件 | 里面只有 `assert True` |
| `app/schemas/spheroidization.py` 里 6 个硬编码 `None`/`False` | 给 DDL 里不存在的列凭空造返回值 |

### 5. 测试

| 文件 | 内容 |
| --- | --- |
| `tests/conftest.py` | pytest 夹具。每个测试一个**临时空库**，不碰 `data/spheroidization.db` |
| `tests/test_smoke.py` | **30 个测试**，直接调 UseCase，覆盖业务规则 + 数据库约束 |
| `tests/e2e_test.py` | **37 项**，走真实 HTTP，最后直查数据库对账（需先起服务） |
| `tests/check_db_connection.py` | 演示脚本：打印「建之前几行 → 通过接口建一条 → 建之后几行」 |

### 6. 其他

- `app/main.py` 的 CORS 补上 `http://127.0.0.1:5173`（原来只有 `localhost`，前端从 127.0.0.1 打开会被拦掉）。
- 修复 `GET /api/standards/current` 空表时 `IndexError` 导致 500 的问题。

---

## 二、验证结果

| 验证项 | 方法 | 结果 |
| --- | --- | --- |
| 测试全过 | `pytest` | **30 passed** |
| 接口全通 | `tests/e2e_test.py` | **37/37 通过** |
| 数据真的落盘 | 写入后杀掉进程，重启再读 | 数据还在 |
| 接口读的是数据库不是 mock | 对比三者 id | API `[1,2,3]` == 数据库 `[1,2,3]` ≠ 原 mock `[1,2,3,4,5]` |
| 业务规则真生效 | 重复处置同一记录 | 返回 `code: 400`，原处理人未被覆盖 |
| 外键真生效 | 删被引用的标准 | 抛 `IntegrityError` |
| 每个 UseCase 都被用到 | 逐个对照路由 | 7 个 UseCase、10 个方法**全部被调用** |
| 每个仓储方法都被用到 | 逐个对照 UseCase | 10 个方法**全部被调用** |

**未验证**：前端页面实际跑起来的样子。以上全部是接口层验证，没有打开过浏览器。

---

## 三、还没做的

### 前后端一起实跑一遍

**这是唯一没验证过的环节。** 需要：起后端 → 起前端 → 打开页面点一遍（列表、详情、处置按钮、异常角标、建标准）。

### 代码里的小尾巴

`app/dependencies.py` 里的 `get_spheroid_repo` / `get_standard_repo` / `get_review_repo` 三个函数**全项目零引用**。它们是为了"直接把仓储注入路由"准备的，但路由只注入 UseCase（这是对的）。写文档时注明用途。

---

## 四、怎么启动后端

在项目根目录下跑：

```bash
cd D:/东贝/spheroidization-system-main
.venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
```

看到这几行就是起好了：

```
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```

**起来之后能开的两个地址：**

| 地址 | 是什么 |
| --- | --- |
| http://127.0.0.1:8000/docs | 接口测试台。网页版，点开任意接口 → "Try it out" → Execute，直接把请求发出去 |
| http://127.0.0.1:8000/api/health | 健康检查，返回 `{"code": 200, ...}` |

**停服务**：在跑服务的那个窗口按 `Ctrl+C`。

**开发时加 `--reload`**：改了代码存盘，服务自动重启，不用手动重开：

```bash
.venv/Scripts/python.exe -m uvicorn app.main:app --port 8000 --reload
```

### 启动的时候自动做了什么

`app/main.py` 里的 `lifespan` 会调一次 `get_container()`，一次性把**建连接 + 建表**都做完。

所以：数据库文件路径不对、`schema.sql` 写错了，**都在启动那一刻就报出来**，不会拖到你发第一个请求才发现。

**数据库文件**：`data/spheroidization.db`。不用手动创建，第一次启动自动生成；表也是自动建的。

### 跑测试

```bash
# 30 个测试，直接调 UseCase，不需要起服务
.venv/Scripts/python.exe -m pytest

# 37 项端到端，走真实 HTTP，需要服务已经起着
.venv/Scripts/python.exe tests/e2e_test.py

# 演示脚本：打印「建之前几行 → 通过接口建一条 → 建之后几行」
.venv/Scripts/python.exe tests/check_db_connection.py
```
